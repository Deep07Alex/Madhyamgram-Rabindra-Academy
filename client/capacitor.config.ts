import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.madhyamgram.rabindra.academy',
  appName: 'Rabindra Academy',
  webDir: 'dist'
};

export default config;
